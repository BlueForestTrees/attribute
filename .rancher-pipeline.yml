stages:
- name: build
  steps:
  - publishImageConfig:
      dockerfilePath: ./Dockerfile
      buildContext: .
      tag: blueforest/attribut:1.1.9
      pushRemote: true
      registry: index.docker.io
timeout: 60
